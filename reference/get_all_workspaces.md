# Get all workspaces

Get all workspaces

## Usage

``` r
get_all_workspaces(token = get_todoist_api_token())
```

## Arguments

- token:

  todoist API token

## Value

tibble of all workspaces

## Examples

``` r
if (FALSE) { # \dontrun{
get_all_workspaces()
} # }
```
