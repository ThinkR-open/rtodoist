# Get users id

Get users id

## Usage

``` r
get_users_id(
  mails,
  all_users = get_all_users(token = token),
  token = get_todoist_api_token()
)
```

## Arguments

- mails:

  mails of the person

- all_users:

  all_users

- token:

  token

## Value

id of users
