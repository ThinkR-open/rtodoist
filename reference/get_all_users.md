# Get users

Get a tibble with emails and ids of users

## Usage

``` r
get_all_users(token = get_todoist_api_token())
```

## Arguments

- token:

  token

## Value

tibble of users

## Examples

``` r
if (FALSE) { # \dontrun{
get_users()
} # }
```
