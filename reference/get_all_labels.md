# Get all labels

Get all labels

## Usage

``` r
get_all_labels(token = get_todoist_api_token())
```

## Arguments

- token:

  todoist API token

## Value

tibble of all labels

## Examples

``` r
if (FALSE) { # \dontrun{
get_all_labels()
} # }
```
