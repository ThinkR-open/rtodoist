# Call the REST API

Call the REST API

## Usage

``` r
call_api_rest(endpoint, token = get_todoist_api_token(), ...)
```

## Arguments

- endpoint:

  API endpoint (e.g., "projects", "tasks")

- token:

  todoist API token

- ...:

  query parameters

## Value

list
