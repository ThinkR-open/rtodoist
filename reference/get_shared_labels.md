# Get shared labels

Get shared labels

## Usage

``` r
get_shared_labels(token = get_todoist_api_token())
```

## Arguments

- token:

  todoist API token

## Value

tibble of shared labels

## Examples

``` r
if (FALSE) { # \dontrun{
get_shared_labels()
} # }
```
